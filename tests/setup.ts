import { beforeAll, afterAll } from '@jest/globals';
import dotenv from 'dotenv';
dotenv.config();

// Global setup before all tests
beforeAll(() => {
  // Can add global mocks or database setup here
});

afterAll(() => {
  // Can add global teardown here
});
